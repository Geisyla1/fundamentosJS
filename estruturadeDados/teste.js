/*let numeros = [20, 35, 45, 78, 98];
numeros.pop();
numeros.unshift(2);
numeros.push(50);
numeros.shift();
//numeros.splice(0,2);
numeros.splice(5,0,5,6,7)
console.log(numeros);
console.log(numeros.length);
*/
const matriz1=[];
function matriz(matriz1){
    for(let i=0;i<matriz1.length;i++){
        for(let j=0;j<matriz1.length;j++){
            console.log(matriz1[i][j]);
        }
    }
}

